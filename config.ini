
[TextLogPatterns]
failed_login = "Failed password"
suspicious_ip = "foreign_ip_list"
privilege_escalation = "sudo|root access granted"
unusual_commands = "rm -rf|chmod|netstat"
sensitive_access = "/etc/passwd|/var/log/auth.log"

[EventLogIDs]
successful_logon = 4624
failed_logon = 4625

[CustomEvents]
# Add additional Event IDs or text patterns here for custom monitoring
